# Copy this to .env.local — never commit .env.local

# UK fuel price feeds (Asda, Tesco, BP, Shell, etc.) need no API key.
# If you have a premium fuel data API, add your credentials below:
# FUEL_API_CLIENT_ID=your_client_id_here
# FUEL_API_CLIENT_SECRET=your_client_secret_here
