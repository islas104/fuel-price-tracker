# Fuel Finder API (UK government — currently in private beta)
# When the API becomes publicly available, add your credentials here.
# Leave blank to use CMA open feeds only (works right now).
# FUEL_FINDER_CLIENT_ID=your_client_id_here
# FUEL_FINDER_CLIENT_SECRET=your_client_secret_here
